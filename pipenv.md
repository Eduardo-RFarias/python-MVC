# Intalar pipenv

```bash
pip install pipenv
```

# Usar pipenv

- Intalar tudo o que falta

```bash
pipenv install --dev
```

- Instalar algo novo

```bash
pipenv install <nome do pacote>
```

ex:

```bash
pipenv install django
```

- Rodar o ambiente virtual

```bash
pipenv shell
```
